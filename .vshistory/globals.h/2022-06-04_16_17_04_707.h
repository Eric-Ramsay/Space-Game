#pragma once
// Global Variables - Yeah Yeah
// These are here to limit parameter list sizes
extern sf::RenderWindow window(sf::VideoMode(1920, 1080), "SP Game", sf::Style::Fullscreen);
extern sf::Texture* sprites();
extern float zoom = .25;